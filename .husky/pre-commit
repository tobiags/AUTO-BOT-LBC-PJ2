#!/usr/bin/env sh
# Husky pre-commit — lint + tests unitaires rapides (<15s)
set -e

echo "==> Lint Python (ruff)"
ruff check app/ tests/

echo "==> Tests unitaires (marqueur unit)"
pytest -m unit -q --tb=short
